import TitleHeader from "@/app/features/titleHeader";

export default function MobileHeader() {
    return (
        <div onClick={event => {
        }}>
            <TitleHeader/>
        </div>
    )
}